[project]
name = "computer-vision-ppe"
version = "0.1.0"
description = "Privacy-aware Personal Protective Equipment (PPE) detection pipeline using YOLO-pose"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "ultralytics>=8.0.0",
    "opencv-python>=4.8.0",
    "numpy>=1.24.0",
    "pynacl>=1.5.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "black>=23.0.0",
]
